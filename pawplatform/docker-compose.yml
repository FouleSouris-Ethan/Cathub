services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: pawuser
      POSTGRES_PASSWORD: pawpassword
      POSTGRES_DB: pawplatform
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
volumes:
  postgres_data: